[project]
name = "evals"
version = "0.1.0"
description = "Eval harness for the marketing-agent /api/generate endpoint"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "pytest>=9.0.3",
]

[tool.uv]
venv = "marketing_agent"
